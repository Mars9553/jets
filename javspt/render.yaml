services:
  - type: web
    name: eboard-pwa
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /
      interval: 60
